# Secrets — never commit
.env

# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Cached ticker universe / scan artifacts
*.cache
data/cache/
data/catalyst_cache.json
data/last_scan.json

# Stray files from accidental shell redirects (e.g. `pip install foo>=7.7`)
=*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
